// Replace 'POST' with the correct exported member from "@/lib/auth", for example 'default' or another named export
// src/app/api/auth/[...nextauth]/route.ts
export { GET, POST } from "@/lib/auth";

